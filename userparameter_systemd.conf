UserParameter=systemd.service.getservices,sys_list=$(systemctl list-unit-files | grep ".service" | awk '{split($0,a,".service"); split($0,b," "); print "\"{#SERVICE}\":\""a[1]"\",\"{#STATE}\":\"" b[2]"\""}'); echo -n '['; for s in $sys_list; do echo -n "{"$s"},"; done | sed 's/.$//' ; echo -n ']'